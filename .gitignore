# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
coverage/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Docker / data
pgdata/
ollama/
*.local

# Keep the shared team brain (memory/) COMMITTED — do not ignore it.

# Next.js generated
next-env.d.ts

# Internal source material (never commit to public repo)
content-source/
