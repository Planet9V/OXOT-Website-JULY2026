# Decisions log

- 2026-07-02 — Repo: Planet9v/OXOT-Website-JULY2026, public.
- 2026-07-02 — Superpowers via obra/superpowers-marketplace.
- 2026-07-02 — Memory: shared, committed, union-merge.
- 2026-07-02 — Valyu: full stack (skill + MCP + SDK) in Docker.
- 2026-07-02 — Generation: local Ollama primary, OpenRouter fallback.
- 2026-07-02 — Embeddings: Ollama Qwen3-4B, 4096-dim, in Postgres/pgvector.
- 2026-07-02 — Native NL + EN mandatory for all user-facing strings.
- 2026-07-02 — Claude Code runs in the `app` dev container (default).
- 2026-07-02 — release.yml builds+pushes image to GHCR only (no deploy yet).
- 2026-07-02 — Scaffolded: Next.js app (nl/en routing, global-token stylesheet, dark/light), lib (db/embeddings/LLM provider w/ Ollama→OpenRouter fallback), pgvector migration 001, agent API (/api/agent stream + citations, /api/session, /api/events consent-gated), retrieval (locale filter + page boost), content ingestion (content/{nl,en} + scripts/ingest.mjs), chat widget (consent + streaming + click/page beacons). Not yet run: npm install / tsc / next build (run in container/CI).
- 2026-07-02 — Superpowers: auto-install via scripts/bootstrap-claude.sh + devcontainer postCreate (claude plugin CLI, --scope project). Karpathy installed as CLAUDE.md law + .claude/skills/karpathy-guidelines.
- 2026-07-02 — Admin/CMS scaffolded: migration 002 (admin_users, pages, menus, menu_items), auth.ts (scrypt + signed-cookie session, AUTH_SECRET), /api/admin/{login,logout,pages,menu-items}, /admin login + guarded dashboard, PagesManager UI. Publish requires both nl+en. create-admin.mjs seeds users.
- 2026-07-02 — Embedding dimension: REAL model qwen3-embedding:4b emits 2560-dim (not 4096 as the brief stated). Made dim env-driven: EMBED_DIM (default 2560) used by embeddings.ts, ingest.mjs, and migration 001 (vector(__EMBED_DIM__) substituted by migrate.mjs). Chat model default set to qwen3:4b. CONFIRM exact tags + dim against the running Ollama and set EMBED_DIM in .env.local before migrating.
- 2026-07-02 — Generation default set to local Ollama qwen3.5:9b (installed on the box), env OLLAMA_CHAT_MODEL. Embeddings qwen3-embedding:4b @ 2560-dim. OpenRouter remains automatic fallback.
- 2026-07-02 — .claude/settings.json now declares Superpowers via extraKnownMarketplaces (obra/superpowers-marketplace) + enabledPlugins {superpowers@superpowers-marketplace:true}. Corrected auto-memory keys to real schema: autoMemoryEnabled + autoMemoryDirectory=./memory (previously used a wrong "memory" object). When the repo folder is trusted in Claude Code it prompts to install; or run: claude plugin install superpowers@superpowers-marketplace.
- 2026-07-02 — Added docs/ARCHITECTURE.md (system-design: reqs, data model, API contracts, retrieval/gen flow, scale, trade-offs, revisit list) and docs/HANDOFF.md (Cowork→Claude Code runbook). Biggest known gap before build: NO TESTS yet (DoD requires TDD) — first Claude Code task.
- 2026-07-02 — GREEN BASELINE verified (in sandbox, local disk): npm install OK (376 pkgs), tsc --noEmit = 0 errors, `next build` = success (14 routes: /nl,/en SSG; [slug]/admin/api dynamic; middleware built). Scaffold is type-clean and builds. Added next-env.d.ts (gitignored per Next convention). NOTE: the node_modules created on the Downloads mount got corrupted by interrupted installs on the network FS — delete it locally (`rm -rf node_modules`) before `npm install`. Build must run on a normal/local FS or inside the container, not directly on the flaky mount.

## 2026-07-06 — first full content + brand + marketing build (shipped to main via PR #1/#2/#3)

- EMBEDDINGS: confirmed real model `qwen3-embedding:4b` = **2560-dim** (not 4096). `EMBED_DIM=2560` in .env.local; migration 001 `vector(__EMBED_DIM__)` substituted by migrate.mjs. pgvector HNSW/IVFFlat cap at 2000 dims, so migration 001's ANN index was **removed** — retrieval uses exact cosine `<=>` (corpus is small; add `halfvec` HNSW when it grows). Fixed the stale "4096-dim" comment.
- DOCKER (local dev): app uses the **Mac's host Ollama** via `docker-compose.override.yml` (`OLLAMA_HOST=http://host.docker.internal:11434`, `extra_hosts host-gateway`); the in-compose `ollama` service is behind a `with-ollama` profile (off by default). DB port is **not host-published** (Mac already runs Postgres on 5432/5433) — reach it with `docker compose exec db psql -U oxot -d oxot`. The `app` container's command runs **`next dev` as PID1 plus a background auto-seed watcher** (see override) so the dev server survives and content publishes itself.
- DEV SERVER GOTCHA: the slim container has no `pgrep`/`ps`. Never guard dev-server start with `pgrep … || npm run dev` — it starts a 2nd server and collides on :3000. Dev server is the container command now; use `docker compose up -d app` to recreate, not detached `exec`.
- CMS RENDERING: page bodies are **Markdown**, rendered by `src/components/markdown.tsx` (dependency-free, server component). Supports: `#`..`####` headings with anchor ids + auto "On this page" TOC, GFM tables, `> [!NOTE|TIP|IMPORTANT|WARNING]` callouts, fenced ```svg (raw inline SVG), ```carousel (→ `src/components/carousel.tsx`), ```html (raw, for video/iframe embeds), images, and inline **bold**/*italic*/`code`/[links]. Trusted-admin content (dangerouslySetInnerHTML for svg/html). Heading ids are de-duplicated.
- CONTENT PIPELINE: author pages as `content/pages/{en,nl}/<slug>.md` with YAML frontmatter (title, meta_title, meta_description, excerpt, og_image, content_type[page|article], published) → `scripts/seed-pages.mjs` (`npm run seed:pages`) upserts into `pages`. The container watcher re-runs seed:pages on any change under content/pages. `content/` (agent-embedding md) is separate from `content/pages/` (CMS pages).
- SEO CMS: migration **004** added meta_title/meta_description/excerpt/og_image/content_type/published_at to `pages`; `[slug]/page.tsx` `generateMetadata` renders them; admin PagesManager has SEO fields; `content_type='article'` appears in the `/[locale]/blog` (Insights) index. Migration **005** re-seeds the main menu incl. **Insights → /blog** (Home·Services·Cyber Digital Twin·Frameworks·Insights·About·Contact, en+nl).
- CONTENT SHIPPED: home hero + 7 services; pages for services, cyber-digital-twin, about, contact; **6 EU framework pages en+nl** (nis2, ai-act, cra, machine-act, iec-62443, ts-50701) — ~3× depth, cited, tables + SVG diagrams + callouts + cross-links; **CRA enriched from OXOT's own `OXOT_Internal/…/OXOT_CRA/Analysis` docs** (CRA↔62443 mapping, product classes, penalties) + a Readiness carousel + the Annex A PDF (`public/media/`) + embedded video; a **founder essay** `cdt-fooled-by-randomness` (content_type article, from the CDT series + Taleb files) in Insights.
- BRAND: palette from the CRA Annex A sheet — **navy #102030 + orange #F07000** accent, editorial serif headlines, uppercase orange kicker + left orange rule. Global stylesheet (`globals.css`) tokens updated (primary = orange, dark bg = navy, `--font-display` serif, `.oxot-kicker`). Hero visual `src/components/hero-visual.tsx` (Cyber Digital Twin SVG). Image spec: `docs/OXOT_BRAND_IMAGE_STYLEGUIDE.md`.
- MARKETING: `marketing/` — CAMPAIGN_PLAN.md (12-week calendar), linkedin-x-posts(.nl).md, carousel-scripts(.nl).md (paste into the builder). Standalone **carousel builder** at `public/tools/carousel-builder.html` (edit slides → export LinkedIn PDF). Social kit is en+nl.
- SECURITY/REPO: internal source material lives in `content-source/` (gitignored) — never committed to the public repo. Large media (`public/media/*.mp4`) gitignored. Superpowers is installed at **user scope** on the Mac (v5.x); it does not appear in Cowork's own skill menu. Doc correction across HANDOFF/FIRST_PROMPT: there is **no `/reload-plugins`** — quit & relaunch `claude` to load plugins.
- STATE: PRs #1 (doc fix), #2 (site+content+brand), #3 (Dutch social + QA), #4 (docs/wiki + README) all merged to `main`. Typecheck clean; pages verified rendering both locales. GitHub About/topics updated; 10-page admin/dev wiki in `docs/wiki/`.
## 2026-07-06 — generation models + deploy

- **OpenRouter key set** in `.env.local` (gitignored; NOT in memory/repo). Live-tested: auth HTTP 200 (valid, $100 limit), and the app-faithful `/chat/completions` call returns grounded, cited answers.
- **Fallback model:** `OPENROUTER_MODEL=x-ai/grok-4.3` (resolves `grok-4.3-20260430`) — live-tested HTTP 200. Also verified working: `qwen/qwen3.7-plus`. Primary generation stays **local Ollama `qwen3.5:9b`**; grok-4.3 is the automatic fallback via the existing `OpenRouterProvider`.
- **Production build GREEN:** full `next build` passes (Linux) — all routes incl `/[locale]/contact` (SSG nl+en), `/api/contact`, `/api/admin/contact`, static `/robots.txt`, dynamic `/sitemap.xml`. Plus tsc clean + 29 Vitest tests green + i18n 60 keys balanced.
- **Deploy:** one-double-click `scripts/deploy.command` (push → docker build/up → migrate(006) → seed:pages → ingest → npm test → SEO/contact/agent smoke → row counts). Non-interactive; optional admin via `OXOT_ADMIN_EMAIL`/`OXOT_ADMIN_PW`. Real bring-up only runs on the Mac (sandbox can't reach host Docker/Ollama/Postgres).

## 2026-07-06 — remediation waves A–D executed (feature/remediation-1-5)

- **#2 LIGHT MODE (done):** added `.oxot-diagram` component class in `globals.css` (fixed navy `#102030` panel + border, light text) and applied it to the ```svg `figure` in `markdown.tsx` (replaced theme-dependent `bg-muted/20`). All 14 inline diagrams now read on light paper. Tables/callouts/carousel/hero-visual already use theme tokens or self-contained navy panels — verified no hard-coded dark-only colours elsewhere.
- **#3 SEO (done):** new `src/lib/seo.ts` (single source: `SITE_URL` from `SITE_URL`/`NEXT_PUBLIC_SITE_URL` env, default `https://oxot.nl`; `alternates()` hreflang incl `x-default`; `organizationJsonLd`/`articleJsonLd`; `ogImageUrl`). Root `layout.tsx` now has `metadataBase`, title template `%s | OXOT`, default description + OpenGraph/Twitter + default OG image. Home `[locale]/page.tsx` has `generateMetadata` + Organization JSON-LD. `[slug]` adds `alternates` + Article JSON-LD for `content_type='article'`. New `src/app/sitemap.ts` (static routes × locales + published CMS pages via `listPublishedRefs()`, hreflang alternates, DB-failure tolerant) and `src/app/robots.ts` (allow all, disallow /admin /api, sitemap ref). Default OG image generated → `public/og/oxot-default.png` (1200×630, navy/orange, ImageMagick from an SVG). Services/about/frameworks stay CMS pages → inherit `[slug]` metadata; only home is bespoke.
- **#1 AGENT (code done; live smoke = Mac):** fixed the hard-coded `pageId="home"` — `ChatWidget` now derives pageId from `usePathname()` (`pageIdFromPath`: "/en"→"home", "/en/nis2"→"nis2"), prop removed from `[locale]/layout.tsx`. Streaming contract audited and correct (Ollama NDJSON deltas → OpenRouter fallback → graceful `[error generating response]`). Live create-admin + agent/CMS smoke run via `scripts/verify-remediation.command` (needs host Ollama+Postgres). Generation still needs `qwen3.5:9b` pulled OR `OPENROUTER_API_KEY` set (still blank — user must set for fallback).
- **#4 CONTACT (done):** migration `006_contact.sql` (`contact_messages`). Pure validation/spam lib `src/lib/contact.ts` (`validateContact` + honeypot `website` field + `checkRateLimit` in-memory per-IP + `hashIp`). `POST /api/contact` (validate → honeypot fake-200 → rate-limit 429 → insert). Bilingual `ContactForm` client component (dictionaries `contact.*` added en+nl, i18n balanced 60 keys) on dedicated `/[locale]/contact/page.tsx` (renders CMS `contact` body above the form; takes precedence over `[slug]`). Admin inbox: `GET/PATCH /api/admin/contact` + `ContactInbox` component in the admin dashboard. Email delivery deferred (no SMTP/Resend key) — stored in DB.
- **#5 TESTS (done, green):** added **Vitest** (`vitest.config.ts`, node env, `@`+`next/headers` aliases, react-dom/server render — no jsdom). `npm test` → `vitest run`. **29 tests pass**: `contact` (validate/honeypot/ratelimit/hashIp), `auth` (password round-trip, session valid/tampered/expired), `markdown` (tables, callouts, diagram panel, **heading-anchor de-dup**, inline bold+link, **unclosed-fence no-loop**, TOC), `frontmatter` (extracted parser `scripts/lib/frontmatter.mjs`, colon-in-value, quotes, no-frontmatter), `retrieval` (mocked pool: SQL `<=>`/`$1::vector`/locale/page-boost/LIMIT + params). Tests run Linux-local; `tests/` + `vitest.config.ts` excluded from app `tsc` (CI installs vitest and runs it via existing `npm test --if-present`). tsc clean.
- **CROSS-CUTTING:** `main` branch protection to be enabled via `scripts/verify-remediation.command` (gh api). All changes ship on `feature/remediation-1-5` via `scripts/ship-remediation.command` (PR, matches prior flow).

- KNOWN GAPS (code-reviewed 2026-07-06) + plan in **`docs/REMEDIATION_PLAN.md`**: (1) the AI chat agent AND the admin CMS were never exercised end-to-end — agent widget hardcodes `pageId="home"` (`[locale]/layout.tsx`); generation needs Ollama qwen3.5:9b reachable OR `OPENROUTER_API_KEY` (blank); no admin user created yet. (2) light mode likely broken for the inline SVG diagrams (light text on light paper; the ```svg figure wrapper is `bg-muted/20`) — fix: give the svg figure a fixed navy panel in `markdown.tsx`. (3) bilingual SEO missing: no hreflang/alternates, no `sitemap.ts`/`robots.ts`, no JSON-LD, no home/services metadata, generic root-layout metadata, no default OG image. (4) contact is a static stub — no form/API/storage. (5) ZERO tests / no test runner (violates CLAUDE.md §8 DoD). Also: not deployed (localhost only), branch protection NOT enforced (PRs self-merged), no security/a11y/mobile review, Dutch + regulatory facts not human/lawyer-reviewed.
